import { BtnDropDownDirective } from './btn-drop-down.directive';

describe('BtnDropDownDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
